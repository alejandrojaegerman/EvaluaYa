# Airtight methodology across the app + PDFs

## What's already consistent (verified)
The live 4-level scale (🟢 verde / 🟡 amarillo / 🟠 naranja / 🔴 rojo) is defined once and reused correctly in:
- `src/lib/assessment-types.ts` (`RiskLevel`), `src/lib/risk.ts` (`RISK_THEME`, `RISK_HEX`), `src/lib/safety-rules.ts` (deterministic rules, incl. Orange tier).
- The AI triage prompt in `src/lib/assessment.functions.ts` (4 levels, Orange = "engineer soon / limit use").
- The **assessment result PDF** (`src/lib/pdf.ts`) — pulls the tag/action from i18n `result.{level}.tag`, so it already renders all 4 levels correctly.
- `/metodologia`, `RiskGauge`, `ConnectEngineers`, `TransparencyBanner`, i18n `result.*` and `methodology.*` keys — all 4-level and aligned with `safety-rules.ts`.

## The real gap (things that are NOT airtight)

### Downloadable technical spec PDFs are the OLD 3-level model
`public/evaluaya-spec-es.pdf` and `evaluaya-spec-en.pdf` (linked from `/metodologia` as the "for engineers" spec) still describe a **Green/Yellow/Red** system and are wrong vs. the current code:
- Title/intro say "decide Verde, Amarillo o Rojo" / "Green, Yellow or Red" — no Orange tier at all.
- Layer‑1 table lists **URM → forces RED**, but code now makes URM *alone* → **Orange** (RED only if URM + damage or moderate shaking).
- "Severe shaking", "spectral demand ≥0.40g", "very soft soil" are listed as **≥ Yellow**, but code now escalates each to **Orange**.
- Layer‑2 (AI) table shows a **3-level** meaning list; combination examples show "URM → Red", "PGA 0.30g → Yellow" etc.

These are generated by `scripts/build-spec-pdf.py` (`content_es`/`content_en`), which is itself outdated.

**Fix:** Update `scripts/build-spec-pdf.py` to match `safety-rules.ts` exactly:
- Retitle/intro to the four levels (🟢🟡🟠🔴).
- Split Layer‑1 into three groups: **→ RED** (liquefaction, pounding, severe plumbing/gas, severe shaking + any structural damage, URM + damage-or-moderate-shaking), **→ ORANGE** (URM alone, severe shaking alone, spectral ≥0.40g, very‑soft soil), **→ ≥ YELLOW** (moderate shaking, soft soil, >7 floors, CMF/CIW/PCF/RML).
- Update the Layer‑2 AI meaning table to 4 rows (add Orange = "moderate‑serious damage; get an engineer soon; limit use") and add an Orange swatch color.
- Update the "how it combines" examples to reflect Orange outcomes (e.g. URM‑alone → Orange, spectral ≥0.4g → Orange).
Then regenerate both PDFs into `public/` and visually QA them (render to images, check tables/colors/wrapping).

## Consistency sweep
After the fix, grep the app for any remaining 3-level phrasing ("Verde, Amarillo o Rojo", "Green, Yellow or Red", "tres niveles/three levels") in routes, components, and i18n, and align any stragglers to the 4-level wording.

## Explicitly out of scope
- **FUNVISIS FAQ (`guia.proceso-oficial-funvisis.tsx`) stays unchanged** — those are separate official guidelines, not EvalúaYa's methodology.
- No changes to scoring or methodology **logic** — `safety-rules.ts`, the AI prompt, and `risk.ts` remain the single source of truth; everything else is brought into agreement with them.

## Technical notes
- Regenerating the spec PDFs runs `python3 scripts/build-spec-pdf.py` (uses reportlab + bundled Vera fonts); output overwrites the two files in `public/`. I'll QA the rendered pages before finishing.
- The assessment/report PDF (`pdf.ts`) needs no code change — it's already data-driven off the 4-level i18n keys.
